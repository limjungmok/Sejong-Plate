
<div class="flexslider">
    <ul class="slides">
        <% @albums.each do |p| %>
            <li>
                <%= image_tag p.picture_url %>
            </li>
        <% end %>
    </ul>
</div>